{
  "name": "@vat-system/tax-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure VAT business logic engine — no infrastructure dependencies",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^2.0.0"
  }
}
